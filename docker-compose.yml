services:
  tracearena:
    build: .
    command: ["uvicorn", "app.demo_server:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
