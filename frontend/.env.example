# 前端环境变量示例（Vite）
# 复制为 .env.local 后按需修改；.env.local 不会进 git。
# 所有变量必须以 VITE_ 前缀开头才会被 Vite 注入到前端代码（import.meta.env）。

# 后端 HTTP API 基地址（登录 / 运营台 / 场景资源 / GLB 模型等都走它）。
# 留空时代码回退到 http://localhost:8001。
VITE_API_BASE=http://localhost:8001

# 后端 WebSocket 地址（实时对局推送 / 控制命令）。
# 留空时代码回退到 ws://127.0.0.1:8001/ws。
VITE_WS_URL=ws://127.0.0.1:8001/ws
